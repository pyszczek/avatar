from django.contrib import admin
from django.urls import path

from . import views

app_name = 'avatarcore'
urlpatterns = [
    # path('activate/<str:user_uuid>/', views.AvatarActivationView.as_view()),
]
